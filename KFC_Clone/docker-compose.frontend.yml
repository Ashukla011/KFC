version: '3.8'
services:
  frontend:
    build:
      context: ./FrontEnd_KFC
      dockerfile: Dockerfile
    container_name: kfc_frontend
    ports:
      - "3000:80"
    environment:
      - BACKEND_URL=http://localhost:5000  # Change to your backend URL if deployed separately
